<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Neety" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/space_cadet</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/isabelline</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/ultra_violet</item>
        <item name="colorSecondaryVariant">@color/space_cadet</item>
        <item name="colorOnSecondary">@color/pale_dogwood</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Default text color. -->
        <item name="android:textColor">@color/space_cadet</item>
        <!-- Customize your theme here. -->

        <!-- Background color. -->
        <item name="backgroundColor">@color/isabelline</item>
        <item name="colorOutline">@color/space_cadet</item>
        <item name="colorPrimaryContainer">@color/pale_dogwood</item>
        <item name="colorSecondaryContainer">@color/pale_dogwood</item>
        <item name="colorOnSecondaryContainer">@color/space_cadet</item>
    </style>
</resources>